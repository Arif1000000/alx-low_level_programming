#include <stdio.h>

/**
 * main - Prints the numbers in base 16
 * Return: Always 0
 */
int main(void)
{
	int al, an;


	for (al = '0'; al <= '9'; al++)
		putchar(al);
	for (an = 97; a <=102' an++)
		putchar(an);
	putchar('\n');

	return (0);
}
